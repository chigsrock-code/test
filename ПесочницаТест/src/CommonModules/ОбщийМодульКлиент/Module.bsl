
Процедура ТестОшибки(Форма) Экспорт
	
	Объект = Форма.Объект;
	НовСтр = Объект.СписокТоваров.Добавить();
    НовСтр.Цена = 111;
	Форма.Элементы.СписокТоваров.ТекущаяСтрока = НовСтр.ПолучитьИдентификатор();

КонецПроцедуры	
